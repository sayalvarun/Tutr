<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.varun.testapp.SignUp"
    android:background="@color/background_color">

    <EditText android:id="@+id/username_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create a Username"
        android:clickable="true"
        android:onClick="clearText"/>
    <EditText android:id="@+id/password_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Create a Password"
        android:layout_below="@id/username_submit"
        android:clickable="true"
        android:onClick="clearPassText"/>
    <EditText android:id="@+id/first_name_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter your first name"
        android:layout_below="@id/password_submit"
        android:clickable="true"
        android:onClick="clearText"/>
    <EditText android:id="@+id/last_name_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter your last name"
        android:layout_below="@id/first_name_submit"
        android:clickable="true"
        android:onClick="clearText"/>
    <EditText android:id="@+id/email_submit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter your e-mail"
        android:layout_below="@id/last_name_submit"
        android:clickable="true"
        android:onClick="clearText"/>

    <Button android:layout_below="@id/email_submit"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="Submit"
        android:id="@+id/submit_form"
        android:onClick="submitFormAction"
        />


</RelativeLayout>
